version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd legal-dashboard
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: legal-dashboard/build
  cache:
    paths:
      - node_modules/**/*